#!/usr/bin/env bash

# dev_clone_repo meetings-client git@github.com:dsm-platform/meetings-client /path/to/clone/to
# $1: reponame
# $2: repourl
# $3: dir to clone to
# $4: branch to checkout


git clone $2 $3
cd $3 && git checkout origin/$4
